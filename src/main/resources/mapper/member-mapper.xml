<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allergyout.member.model.dao.MemberMapper">

    <select id="findByMemberId" resultType="com.allergyout.member.model.vo.Member">
        SELECT
            MEMBER_NO,
            MEMBER_ID,
            MEMBER_PWD,
            MEMBER_NAME,
            PHONE,
            EMAIL,
            MEMBER_IMG,
            ROLE,
            CREATE_DATE,
            DEL_YN
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
          AND DEL_YN = 'N'
    </select>

    <select id="existsByMemberId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
    </select>

    <insert id="insert" parameterType="com.allergyout.member.model.vo.Member">
        <selectKey keyProperty="memberNo" resultType="long" order="BEFORE">
            SELECT SEQ_MEMBER_NO.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO MEMBER (
            MEMBER_NO, MEMBER_ID, MEMBER_PWD, MEMBER_NAME, PHONE, EMAIL
        ) VALUES (
            #{memberNo}, #{memberId}, #{memberPwd}, #{memberName}, #{phone}, #{email}
        )
    </insert>

</mapper>
